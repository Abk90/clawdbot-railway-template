[build]
builder = "dockerfile"

[deploy]
startCommand = "bash openclaw-boot.sh"
healthcheckPath = "/setup/healthz"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[variables]
PORT = "8080"
